# AlgoSpec - Next.js версия с SEO оптимизацией

## ✅ Миграция завершена!

Приложение успешно мигрировано на Next.js с полной SEO оптимизацией.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Файл `.env.local` уже создан. Если нужно изменить URL бэкенда:
```
NEXT_PUBLIC_API_URL=http://localhost:3001/auth
```

### 3. Запуск бэкенда

```bash
cd backend
npm run start:dev
```

### 4. Запуск Next.js приложения

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

## 📁 Структура проекта

```
project-algospec/
├── app/                    # Next.js App Router
│   ├── layout.js          # Корневой layout с SEO мета-тегами
│   ├── page.js            # Главная страница (/)
│   ├── login/page.js      # Страница входа (/login)
│   ├── problems/page.js   # Страница задач (/problems)
│   ├── profile/page.js    # Страница профиля (/profile)
│   ├── globals.css        # Глобальные стили
│   ├── providers.js       # Провайдеры для клиентских компонентов
│   └── sitemap.js         # Автоматическая генерация sitemap
├── components/            # React компоненты
│   ├── Header/           # Шапка сайта
│   ├── Hero/             # Главный баннер
│   ├── Features/         # Особенности курса
│   ├── Companies/        # Компании
│   ├── Topics/           # Темы задач
│   ├── Languages/        # Языки программирования
│   ├── Pricing/          # Цены
│   ├── Footer/           # Подвал
│   └── ProtectedRoute/   # Защищенный роут
├── pages/                 # Компоненты страниц
│   ├── Login.js          # Компонент страницы входа
│   ├── Problems.js       # Компонент страницы задач
│   └── Profile.js        # Компонент страницы профиля
├── context/               # Context провайдеры
│   └── AuthContext.js    # Контекст аутентификации
├── api/                   # API клиенты
│   └── auth.js           # API для аутентификации
├── public/                # Статические файлы
│   ├── img/              # Изображения
│   ├── fonts/            # Шрифты
│   └── robots.txt        # Robots.txt
├── backend/               # NestJS бэкенд
├── next.config.js         # Конфигурация Next.js
└── jsconfig.json          # Алиасы путей
```

## ✨ SEO преимущества Next.js

1. **Server-Side Rendering (SSR)** - страницы рендерятся на сервере
2. **Мета-теги** - уникальные для каждой страницы
3. **Open Graph** - для социальных сетей
4. **Twitter Cards** - для Twitter
5. **Автоматическая оптимизация** - изображений, шрифтов, кода
6. **Sitemap.xml** - автоматическая генерация
7. **Robots.txt** - управление индексацией

## 🔧 Команды

```bash
# Разработка
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен версии
npm start

# Линтинг
npm run lint
```

## 📝 Важные изменения

1. **Роутинг**: Файловая система вместо React Router
2. **Link**: `next/link` вместо `react-router-dom`
3. **Навигация**: `useRouter` из `next/navigation`
4. **Изображения**: `next/image` для оптимизации
5. **Мета-теги**: Экспорт `metadata` из каждой страницы
6. **Клиентские компоненты**: Помечены `'use client'`

## 🌐 SEO мета-теги

Каждая страница имеет свои мета-теги:
- **Главная**: Описание курса, ключевые слова
- **Задачи**: Описание списка задач
- **Вход/Профиль**: robots: noindex (не индексируются)

## 🔐 Аутентификация

- JWT токены хранятся в localStorage
- Защищенные роуты через `ProtectedRoute`
- Автоматическая проверка токена при загрузке

## 📦 Зависимости

- Next.js 14+
- React 18+
- NestJS бэкенд (отдельно)

## 🎯 Готово к использованию!

Все компоненты мигрированы, SEO оптимизация настроена, приложение готово к запуску.

