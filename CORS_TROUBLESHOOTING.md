# Решение проблем с CORS

## 🔍 Диагностика проблемы CORS

### 1. Проверьте логи бэкенда

После обновления кода, бэкенд будет логировать все CORS запросы. Проверьте логи:

```bash
pm2 logs algospec-backend
```

Вы увидите сообщения вида:
```
[CORS] Request from origin: http://123.45.67.89:3000
[CORS] Allowed origins: http://123.45.67.89,http://123.45.67.89:3000
[CORS] Origin http://123.45.67.89:3000 is allowed
```

Или если origin не разрешен:
```
[CORS] ❌ Origin http://123.45.67.89:3000 is NOT allowed
[CORS] Allowed origins: http://123.45.67.89
```

### 2. Проверьте точное совпадение origin

**Важно:** Origin должен точно совпадать, включая:
- Протокол (http:// или https://)
- Домен/IP адрес
- Порт (если указан)

**Примеры правильных настроек:**

Если фронтенд работает на `http://123.45.67.89:3000`:
```env
ALLOWED_ORIGINS=http://123.45.67.89:3000
```

Если фронтенд работает на `http://123.45.67.89` (через Nginx на порту 80):
```env
ALLOWED_ORIGINS=http://123.45.67.89
```

Если фронтенд работает на `https://123.45.67.89`:
```env
ALLOWED_ORIGINS=https://123.45.67.89
```

### 3. Частые ошибки

#### Ошибка 1: Не указан порт
**Проблема:**
- Фронтенд работает на `http://123.45.67.89:3000`
- В ALLOWED_ORIGINS указано: `http://123.45.67.89` (без порта)

**Решение:**
```env
ALLOWED_ORIGINS=http://123.45.67.89:3000
```

#### Ошибка 2: Неправильный протокол
**Проблема:**
- Фронтенд работает на `https://123.45.67.89`
- В ALLOWED_ORIGINS указано: `http://123.45.67.89` (HTTP вместо HTTPS)

**Решение:**
```env
ALLOWED_ORIGINS=https://123.45.67.89
```

#### Ошибка 3: Несколько origins через запятую
**Проблема:**
- Пробелы после запятой
- Неправильный формат

**Правильно:**
```env
ALLOWED_ORIGINS=http://123.45.67.89,http://123.45.67.89:3000
```

**Неправильно:**
```env
ALLOWED_ORIGINS=http://123.45.67.89, http://123.45.67.89:3000  # пробелы после запятой
ALLOWED_ORIGINS="http://123.45.67.89, http://123.45.67.89:3000"  # кавычки
```

### 4. Проверка через браузер

Откройте консоль браузера (F12) и проверьте:

1. **Network tab** - посмотрите на запросы к API
2. **Console tab** - посмотрите на ошибки CORS

Типичная ошибка CORS:
```
Access to fetch at 'http://123.45.67.89:3001/auth/login' from origin 'http://123.45.67.89:3000' 
has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present on the requested resource.
```

### 5. Проверка через curl

Проверьте, что бэкенд отвечает с правильными CORS заголовками:

```bash
curl -H "Origin: http://123.45.67.89:3000" \
     -H "Access-Control-Request-Method: POST" \
     -H "Access-Control-Request-Headers: Content-Type" \
     -X OPTIONS \
     http://123.45.67.89:3001/auth/login \
     -v
```

Должны быть заголовки:
```
Access-Control-Allow-Origin: http://123.45.67.89:3000
Access-Control-Allow-Methods: GET, POST, PUT, DELETE, PATCH, OPTIONS
Access-Control-Allow-Headers: Content-Type, Authorization, Accept
Access-Control-Allow-Credentials: true
```

### 6. Проверка переменных окружения

Убедитесь, что переменная окружения правильно загружена:

```bash
# На сервере
cd backend
cat .env | grep ALLOWED_ORIGINS

# Проверьте, что бэкенд видит переменную
pm2 logs algospec-backend | grep "Allowed CORS origins"
```

### 7. Решение для разных сценариев

#### Сценарий 1: Фронтенд и бэкенд на одном IP, разные порты
```env
# backend/.env
ALLOWED_ORIGINS=http://123.45.67.89:3000

# .env.local (фронтенд)
NEXT_PUBLIC_API_URL=http://123.45.67.89:3001/auth
```

#### Сценарий 2: Фронтенд через Nginx на порту 80, бэкенд на 3001
```env
# backend/.env
ALLOWED_ORIGINS=http://123.45.67.89

# .env.local (фронтенд)
NEXT_PUBLIC_API_URL=http://123.45.67.89/api/auth
```

#### Сценарий 3: HTTPS с самоподписанным сертификатом
```env
# backend/.env
ALLOWED_ORIGINS=https://123.45.67.89

# .env.local (фронтенд)
NEXT_PUBLIC_API_URL=https://123.45.67.89/api/auth
```

### 8. Временное решение (только для тестирования!)

Если нужно быстро протестировать, можно временно разрешить все origins:

```typescript
// backend/src/main.ts (ВРЕМЕННО, только для тестирования!)
app.enableCors({
  origin: true,  // Разрешить все origins
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH', 'OPTIONS'],
  allowedHeaders: ['Content-Type', 'Authorization', 'Accept'],
  credentials: true,
});
```

**⚠️ ВНИМАНИЕ:** Никогда не используйте это в production! Это серьезная уязвимость безопасности.

### 9. Проверка после изменений

После изменения `.env` файла:

```bash
# Перезапустите бэкенд
pm2 restart algospec-backend

# Проверьте логи
pm2 logs algospec-backend --lines 50
```

Должны увидеть:
```
Backend server running on port 3001
Allowed CORS origins: http://123.45.67.89:3000
```

### 10. Если ничего не помогает

1. Проверьте, что бэкенд действительно запущен:
   ```bash
   pm2 status
   curl http://123.45.67.89:3001/auth/profile
   ```

2. Проверьте файрвол:
   ```bash
   sudo ufw status
   sudo ufw allow 3001/tcp
   ```

3. Проверьте, что Nginx не блокирует запросы (если используете)

4. Проверьте логи Nginx:
   ```bash
   sudo tail -f /var/log/nginx/error.log
   ```

---

**Нужна помощь?** Проверьте логи бэкенда и убедитесь, что origin точно совпадает с тем, что указано в ALLOWED_ORIGINS.

